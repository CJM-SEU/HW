# DPV `main.cpp` 乘法器抽象实现解读

## 1. 结论先行

DPV 的乘法器抽象不是把所有 `mul` 节点简单替换掉，而是识别“规格侧字级乘法”与“RTL 侧经过拆分、移位、部分积求和或门级展开的乘法实现”之间的对应关系，再用严格证明为候选授权，最后把 RTL 复杂锥重写成一个字级乘法节点。[VERIFY: src/main.cpp:1275]

它采用两条互补路线：`algebra` 识别共享操作数的部分积求和并利用分配律直接重写；`sca` 用约束合法样本寻找位级/混合粒度候选，再依次交给穷举、ArisCA、条件化 ArisCA 或 RevSCA 验证。[VERIFY: src/main.cpp:1282]

最重要的方法论边界是：采样与指纹匹配只负责缩小搜索空间，不构成正确性证明；除精确代数恒等式外，正常模式下只有形式验证成功的候选才进入 `applied` 并改写模型。[VERIFY: src/main.cpp:1768]

## 2. 在整个求解流水线中的位置

`main.cpp` 给出的顺序是：属性锥 DCE、约束可满足性检查、乘法器抽象、算术等价简化、最终性质求解；乘法器抽象的输出会成为后续简化与最终求解的输入。[VERIFY: src/main.cpp:3]

进入抽象前，程序已经只保留选定 `bad` 的相关锥，并通过常量传播折叠由属性选择产生的固定模式分支，因此不可达的乘法器分支通常不会进入抽象阶段。[VERIFY: src/main.cpp:1179]

程序用 `cur` 始终指向当前版本模型，而 `model` 保留原模型以维持原始属性编号；抽象结果保存在 `abstracted` 中。[VERIFY: src/main.cpp:1084]

```text
原始 BTOR2
   │
   ├─ 选择单个 bad + DCE
   ├─ 恢复 case guard 为 constraint（若可识别）
   ├─ 常量传播 + 再次 DCE
   ├─ assume 区域可满足性检查
   ▼
乘法器抽象
   ├─ 候选发现：algebra / SCA
   ├─ 候选证明：恒等式 / 穷举 / ArisCA / RevSCA
   └─ 重写：RTL 锥输出 → 字级 A' * B'
   ▼
verify+simplify → final solve
```

该顺序有一个直接目的：先把 RTL 侧的复杂乘法结构恢复为字级 `mul`，后续等价简化便可以在规格侧与 RTL 侧的两个字级乘法之间建立关系，而不用直接处理庞大的门级锥。[VERIFY: src/main.cpp:1276]

## 3. 用户可控策略与默认配置

抽象默认开启，`--no-abstract` 可关闭；`--abstract-methods` 控制 `algebra`、`sca` 两类发现方法，帮助文本表明两者默认同时启用。[VERIFY: src/main.cpp:187]

`--abstract-patterns` 决定用于“发现”而非“证明”的样本数，默认值与普通 `-n/--patterns` 解耦，因为样本过少可能把错误操作数提名出来，随后又被全空间验证正确拒绝，最终表现为漏抽象。[VERIFY: src/main.cpp:251]

`--abstract-sample-timeout` 限制发现阶段采样时间；`--sca-timeout` 给每个 SCA 候选的证明器设置局部预算；这些局部预算还会被共同的预处理剩余预算截断。[VERIFY: src/main.cpp:258]

窄乘法锥默认先做全空间穷举，`--no-exhaustive` 可跳过，`--exhaustive-bits` 决定允许穷举的最大操作数总位数。[VERIFY: src/main.cpp:260]

ArisCA 默认启用，`--no-arisca` 可关闭；RevSCA 默认关闭，需要 `--revsca` 显式开启，而且只作为 ArisCA 未定且两操作数等宽时的回退。[VERIFY: src/main.cpp:2019]

`--verify-algebraic` 会要求本可由分配律直接授权的结构候选也经过 SCA 验证；默认不做这一步，因为代码把精确结构恒等式视为比外部 SCA 更直接的证明。[VERIFY: src/main.cpp:1716]

`--risky-merge` 是明确标注为不可靠的逃生选项：只有在候选既未证明也未被反例否定时才允许假定其正确；被明确 `REFUTED` 的候选绝不会因此被抽象。[VERIFY: src/main.cpp:2067]

## 4. 核心数据结构：`MulAbstraction`

一个候选同时记录规格侧乘法节点 `mul`、其两个操作数 `mul_arg`、RTL 输出 `opt_out`、输出/输入移位关系以及 RTL 侧操作数表示。[VERIFY: src/abstraction.hpp:27]

字级候选直接使用 `opt_arg[0]` 和 `opt_arg[1]`；位级或混合粒度候选使用 `opt_part[2]`，每个 `OperandPart` 说明源节点、在重建操作数中的偏移、宽度以及单比特是否取反。[VERIFY: src/abstraction.hpp:39]

候选应满足以下样本关系：

```text
M = A × B
value(M) = value(S) << out_shift
value(A) = value(A') << in_shift[0]
value(B) = value(B') << in_shift[1]
in_shift[0] + in_shift[1] = out_shift
```

这一移位一致性来自乘法的尺度关系：若 `A = A'·2^sa` 且 `B = B'·2^sb`，则 `A·B = A'·B'·2^(sa+sb)`。[VERIFY: src/abstraction.hpp:7]

代数候选设置 `algebraic=true`，其第一操作数不是普通拼接，而是 `Σ(X_i << k_i)`；第二操作数是所有部分积共享的 `B`。[VERIFY: src/abstraction.hpp:55]

## 5. `main.cpp` 的总控流程

### 5.1 构造发现选项

抽象块先从命令行方法集合计算 `use_algebra` 和 `use_sca`，再把样本数、种子、并行度、采样时间、是否定向补样等写入 `AbstractionOptions`。[VERIFY: src/main.cpp:1282]

若只启用代数法，`stop_after_wide_algebraic` 为真，发现阶段采用便宜的“最深优先结构固定点”；启用 SCA 时则进入完整采样发现路径。[VERIFY: src/main.cpp:1300]

当后续 guarantee 简化也开启且使用 SCA 时，`retain_stimulus` 会保存已经付出 SMT 成本取得的合法样本，供后续重写模型重新仿真使用。[VERIFY: src/main.cpp:1303]

### 5.2 首轮候选发现与饥饿重试

主程序调用 `find_mul_abstractions(*cur, ao, &astat)`，候选放入 `cands`，采样和筛选统计放入 `astat`。[VERIFY: src/main.cpp:1305]

如果 SCA 没有候选、采样超时、约束区域仍被认为可满足且合法帧少于 2，主程序把它识别为资源饥饿而非正常“无候选”，扩大一次局部时限、改变种子并只重试一次。[VERIFY: src/main.cpp:1313]

这体现了“只对可诊断的资源失败做有限恢复”的策略：正常无候选和约束不可满足不会触发无界重试。[VERIFY: src/main.cpp:1308]

### 5.3 guard-pinned 稀有路径恢复

若普通 SCA 仍无候选，程序可通过 `find_oracle_cut_points` 搜索 ITE 步进链上的宽、无字级乘法 computed branch，收集其 guard 集合。[VERIFY: src/main.cpp:1365]

该回退只在链深至少为 4、候选值宽至少 16 位时考虑，并最多尝试 3 个 guard 集合，每条嵌套 guard 链最多向内追踪 4 层。[VERIFY: src/main.cpp:1382]

保留样本会先做两种筛选：guard 集在样本中是否联合激活，以及激活时某个规格乘法值是否与 nominated computed value 相差一个二的幂移位；无活动或无乘法匹配的集合会被丢弃。[VERIFY: src/main.cpp:1466]

通过筛选的集合按置信度、是否能容纳在最宽规格乘法中、值宽和 guard 深度排序，随后临时把 guard 作为额外约束并重新运行候选发现。[VERIFY: src/main.cpp:1609]

guard pin 只缩小合法输入区域，不能直接授权重写；由此发现的候选仍必须走后续全空间或条件化证明，因此错误提名最多浪费有界时间，不改变正常模式的可靠性。[VERIFY: src/main.cpp:1373]

## 6. 候选发现算法的内部方法

### 6.1 路线 A：严格代数快路径

`find_mul_abstractions` 首先计算逻辑层级，然后在允许代数快路径时调用 `find_wide_algebraic_fastpath`；找到一个候选便立刻返回，不生成样本。[VERIFY: src/abstraction.cpp:1229]

快路径只在活动 `bad` 锥中寻找根节点，并用一个封顶为 2 的 `mulsum_count` 自底向上预筛选包含至少两个乘法叶子的 `add/sll/slice/uext/concat` 结构。[VERIFY: src/abstraction.cpp:401]

候选根按逻辑层级从深到浅排列，以优先折叠最接近性质端、影响最大的宽乘法结构。[VERIFY: src/abstraction.cpp:450]

严格分解接受 `mul`、`add`、常量左移、低位起始的 `slice`、零扩展以及单侧零填充 `concat`，并持续记录每个部分积到根节点时实际保留的低位数。[VERIFY: src/abstraction.cpp:257]

对于每个部分积，算法要求存在共同操作数 `B`，并要求 `effective_width(X_i)+effective_width(B)` 不超过该部分积实际保留位数，避免把已截断且可能丢失有效位的部分积当作完整乘法。[VERIFY: src/abstraction.cpp:485]

快路径还要求各 `X_i << k_i` 的有效范围不重叠，并要求组合操作数与 `B` 的有效宽度之和能被输出宽度容纳。[VERIFY: src/abstraction.cpp:509]

满足这些条件时使用分配律：

```text
S = Σ ((X_i × B) << k_i)
  = Σ ((X_i << k_i) × B)
  = (Σ (X_i << k_i)) × B
```

候选因此可以把部分积加法树恢复为单个字级乘法，而不需要依赖有限样本推断。[VERIFY: src/abstraction.cpp:581]

### 6.2 路线 B：约束合法采样与行为指纹

只有显式启用 SCA 且代数快路径没有提前返回时，函数才继续做昂贵发现；模型中没有任何 `mul` 时也会直接返回。[VERIFY: src/abstraction.cpp:1248]

约束模式下通过 `sample_constraint_legal` 生成满足全部 BTOR2 constraints 的赋值；约束区域不可满足或不足两个样本时停止发现。[VERIFY: src/abstraction.cpp:1272]

每个样本在全模型上求值，所有节点形成“值列”；代码用带样本序号盐的值哈希并跨样本 XOR，得到节点行为指纹，同时保存每个乘法器、其操作数和全部输入的精确列。[VERIFY: src/abstraction.cpp:1312]

代码还对每个值去掉尾随零后计算“奇数部分指纹”，用于发现两个输出之间存在随样本变化的二次幂移位关系。[VERIFY: src/abstraction.cpp:1319]

若某个乘法器输出或操作数在已有样本中几乎不变化，它无法被可靠区分，还会与大量常量列碰撞；因此代码会调用 `steer_at_dead_muls`，定向让这些乘法器的操作数变化，再重新仿真。[VERIFY: src/abstraction.cpp:1366]

### 6.3 指纹提名与精确列复核

每个规格乘法 `M` 会在 0 到 96 位移范围内查找 `M >> s` 的指纹桶，形成可能满足 `M = S << s` 的输出候选；操作数候选也以同样方式查找。[VERIFY: src/abstraction.cpp:1497]

对于变量移位，候选首先由奇数部分指纹提名，再逐样本计算商，要求商无余数且恰为二的幂，同时要求观察到至少两个、至多配置上限个不同移位量。[VERIFY: src/abstraction.cpp:1563]

指纹只是索引：第二遍会仅为被提名节点收集精确值列，并逐样本执行 `wide[p] == narrow[p] << s` 检查，从而排除哈希碰撞和偶然桶命中。[VERIFY: src/abstraction.cpp:1558]

常量或低区分度目标会被拒绝，昂贵输出查找还受 `max_bucket` 限制，避免一个超大等价桶导致为大量候选反复构造影响锥。[VERIFY: src/abstraction.cpp:1436]

### 6.4 字级操作数匹配

对输出候选 `S`，算法拒绝 `S` 位于规格乘法锥内或规格乘法位于 `S` 锥内的循环/非独立情形，并要求 `S` 至少依赖一个随样本变化的自由输入。[VERIFY: src/abstraction.cpp:1718]

字级匹配要求 `A'` 与 `B'` 都在 `S` 的锥内、分别与规格操作数满足精确移位关系、两个自由输入锥互不相交，并且二者恰好覆盖 `S` 的全部变化自由输入。[VERIFY: src/abstraction.cpp:1840]

同时要求 `in_shift(A') + in_shift(B') == out_shift(S)`，从结构和数值两方面约束候选操作数映射。[VERIFY: src/abstraction.cpp:1850]

### 6.5 位级/混合粒度操作数重建

若找不到合适的字级操作数，算法尝试用多个连续片段重建两个操作数；门级网表是每个片段宽度为 1 的特例，混合表示则允许宽片段与单比特片段并存。[VERIFY: src/abstraction.cpp:1892]

每个片段通过样本值匹配到某一操作数的连续位区间，单比特片段还允许反相；最终每侧片段必须从偏移 0 开始无空洞地铺满整个重建操作数。[VERIFY: src/abstraction.cpp:2916]

对常为零的低位，算法可以不把它们作为真实阵列输入，而记录为 `in_shift`；重建后逐样本验证 `R == ((A'×B') << (lzA+lzB)) mod 2^wR`。[VERIFY: src/abstraction.cpp:2943]

代码还要求所选片段真正切断输出锥：如果从输出向下越过切点后仍能到达非恒定主输入，说明片段没有完整描述乘法器边界，该候选会被拒绝。[VERIFY: src/abstraction.cpp:2791]

## 7. 候选证明与授权阶梯

### 7.1 代数候选

默认情况下，严格共享操作数乘法和候选直接加入 `applied`，因为重写本身就是经结构条件约束的分配律恒等变换；开启 `--verify-algebraic` 才继续走提锥和外部验证。[VERIFY: src/main.cpp:1721]

### 7.2 独立乘法锥提取

非直接授权候选先调用 `extract_multiplier_cone`，把 RTL 乘法边界切成独立组合 BTOR2 模型；字级候选把 `A'`、`B'` 变成新输入，位级候选则按 A 低位到高位、再 B 低位到高位的顺序声明片段输入。[VERIFY: src/abstraction.cpp:1085]

输出外围的恒零拼接会被剥离并记录为 `out_shift`，因为外部 SCA 验证覆盖完整布尔输入空间，输出中不能混有未纳入乘法规格的固定填充位。[VERIFY: src/abstraction.cpp:839]

除操作数外的辅助输入只有在 Bitwuzla 证明其在当前约束空间恒定时才会折叠为常量；求解 `UNKNOWN` 时保留为自由输入，从而避免依据单个样本做不可靠常量折叠。[VERIFY: src/abstraction.cpp:865]

若位级候选的乘积在输出中上移，ArisCA 只证明上部切片，因此主程序还用 `low_bits_zero` 证明低 `shift` 位在全部合法输入上恒为零。[VERIFY: src/main.cpp:1747]

`low_bits_zero` 的形式查询是：断言原 constraints，再断言输出低位不等于零；只有该查询为 `UNSAT` 才返回真。[VERIFY: src/abstraction.cpp:3146]

### 7.3 窄锥全空间穷举

证明阶梯首先调用 `exhaustive_multiplier_check`；只有操作数总位数不超过阈值时才枚举整个输入空间，并利用配置的分析线程并行执行。[VERIFY: src/main.cpp:1773]

返回 `PROVED` 即授权，返回实际检查后的非 `PROVED` 即视为 `REFUTED`；`TOO_LARGE` 和 `TIMED_OUT` 不作为反例，只继续或保持未定。[VERIFY: src/main.cpp:1784]

### 7.4 ArisCA

当仍未决定时，程序把独立 BTOR2 锥输出为 ASCII AIGER，并依据 AIG support 计算两个操作数在输入向量中的有效跨度。[VERIFY: src/main.cpp:1799]

发送给 ArisCA 的规格形如 `o[wo]=[wa:oa]*[wb:ob]`；任一操作数完全不在输出 support 中时跳过，因为它不可能是有效乘法输入。[VERIFY: src/main.cpp:1888]

ArisCA 的 `PROVED` 设置 `proved=true`，`REFUTED` 设置 `refuted=true`，`UNKNOWN/UNAVAILABLE` 不授权重写。[VERIFY: src/main.cpp:1921]

### 7.5 约束空间中的条件化 ArisCA

位级候选可能只在属性的合法约束空间内是乘法器，例如某个隐藏最高位被 case guard 固定；全布尔空间 ArisCA 此时会正确地否定无条件命题。[VERIFY: src/main.cpp:1938]

主程序用 `constraint_constant_operand_bits` 找到并逐个证明常量操作数位，再构造“guard × 输出 = guard × 乘积”的条件恒等式交给 ArisCA。[VERIFY: src/main.cpp:1951]

只有外部 SCA 证明条件恒等式，并且 Bitwuzla 已证明对应条件对全部合法输入成立时，才把先前的无条件 `REFUTED` 转为合法空间内的 `PROVED`。[VERIFY: src/main.cpp:2011]

### 7.6 RevSCA 回退

当候选既未证明也未否定、显式启用 RevSCA 且两操作数等宽时，程序把锥写为 binary AIGER 并调用 RevSCA；只有 `CORRECT` 才授权。[VERIFY: src/main.cpp:2024]

## 8. 模型重写如何发生

所有获授权候选汇总到 `applied`，随后一次调用 `emit_abstracted_model` 生成新 BTOR2 文本，再重新解析为 `Model` 并让 `cur` 指向新模型。[VERIFY: src/main.cpp:2095]

重写器在操作数最后定义的位置之后插入替代逻辑，确保新节点满足 BTOR2 的先定义后使用顺序；原 RTL 输出的后续消费者通过 `opt_to_rep` 被重定向到新乘法结果。[VERIFY: src/abstraction.cpp:611]

位级普通操作数按偏移顺序用 `concat` 重建；代数操作数则将各片段零扩展、左移后用 `add` 重建，因为部分范围允许重叠，不能等价地视作拼接。[VERIFY: src/abstraction.cpp:717]

重建操作数会被零扩展到输出宽度，或在更宽时截取低位；随后发射模块乘法 `mul`，因此自然支持输出宽度小于两操作数宽度之和的截断乘积。[VERIFY: src/abstraction.cpp:801]

若低零位曾从操作数中剥离，重写器在字级乘积后补上对应左移，使替代节点与原 `opt_out` 的位位置一致。[VERIFY: src/abstraction.cpp:818]

完成首批替换后，若走的是纯严格代数快路径，主程序会在新模型上重新扫描并一次重写一个候选，最多 32 轮，直到找不到候选或重写不再产生消费者重定向。[VERIFY: src/main.cpp:2110]

## 9. 方法论总结

### 9.1 “发现”和“证明”分层

行为指纹把原本近似平方级的全节点对比较转成哈希桶提名，再用精确列检查消除哈希碰撞；形式验证只处理少量候选锥，而不是整个模型。[VERIFY: src/abstraction.cpp:1414]

这种分层允许发现阶段追求召回率，例如允许变量移位、位级拼接、稀有 guard 重试；可靠性仍由后续精确证明门槛控制。[VERIFY: src/main.cpp:1373]

### 9.2 “结构证据优先，统计证据辅助”

严格分配律结构满足完备位宽条件时直接重写；结构不明显时才使用约束合法采样恢复边界，降低大模型上的采样与外部验证成本。[VERIFY: src/abstraction.cpp:1233]

### 9.3 “约束感知，但不把样本当约束证明”

采样必须满足模型 constraints，稀有分支可通过 guard pin 或 operand steering 激活；但常量折叠、低位恒零和条件 guard 都需要 Bitwuzla 的 `UNSAT` 结论才能进入可靠重写。[VERIFY: src/abstraction.cpp:1274]

### 9.4 “便宜决策优先，昂贵后端兜底”

候选验证顺序为：精确代数恒等式 → 窄锥全空间穷举 → ArisCA → 条件化 ArisCA → 可选 RevSCA；每一级都只在前一级没有给出足够结论时继续。[VERIFY: src/main.cpp:1716]

### 9.5 “渐进重写与资源闭环”

每次重写后重新解析模型，纯代数模式继续扫描到固定点；采样结果会交给后续 guarantee 简化复用，重写发生后还会停止已经落后于预处理的 early-solve 子进程。[VERIFY: src/main.cpp:2172]

## 10. 正确性边界与常见误读

1. “样本上相等”不等于“可抽象”：样本仅用于候选生成，普通 SCA 候选仍需全空间或约束许可的形式证明。[VERIFY: src/main.cpp:2080]
2. `algebraic` 不是经验优化标签，而是具有共享操作数、保留位宽与非重叠有效范围条件的具体分配律重写。[VERIFY: src/abstraction.cpp:497]
3. ArisCA 的无条件 `REFUTED` 不一定否定约束空间身份；仅位级非代数候选可以在证明 guard 恒定后尝试条件化恒等式。[VERIFY: src/main.cpp:1947]
4. `--risky-merge` 会越过“必须证明”的边界，因此启用后的结果不再具有默认流程的声音性保证。[VERIFY: src/main.cpp:2072]
5. 抽象并不删除原锥文本本身，而是把消费者改接到新字级乘法；后续 DCE 才能清除失去消费者的旧实现逻辑。[VERIFY: src/abstraction.hpp:224]

## 11. 一页式执行流程

```text
if --no-abstract 或预处理预算耗尽:
    跳过
else:
    1. 配置 algebra/SCA、样本、steering、预算
    2. find_mul_abstractions
       2.1 严格代数快路径
       2.2 若启用 SCA：合法采样 → 全图指纹 → dead-mul steering
       2.3 指纹提名 → 精确值列复核
       2.4 字级边界匹配 / 位级片段铺设
    3. 若采样明显饥饿，仅回退重试一次
    4. 若仍无候选且存在长 oracle 链，筛选并 pin guard 后重试
    5. 对每个候选：
       5.1 精确代数候选默认直接授权
       5.2 提取独立乘法锥
       5.3 必要时证明输出低位恒零
       5.4 窄锥穷举
       5.5 ArisCA
       5.6 位级候选的条件化 ArisCA
       5.7 可选 RevSCA
       5.8 仅 PROVED/CORRECT（或显式 risky）加入 applied
    6. emit_abstracted_model：重建操作数、插入 mul、重定向消费者
    7. 纯代数模式继续扫描到固定点
    8. 新模型进入 guarantee simplification 和 final solve
```

该流程的本质可概括为：用便宜的结构与仿真分析定位“哪里像乘法器”，用局部而严格的证明回答“它在所需语义域内是否真是乘法器”，再用字级节点替换复杂 RTL 锥以降低后续等价求解成本。[VERIFY: src/main.cpp:1275]

## 12. 复核记录

- 已核对 `main.cpp` 中抽象入口、资源预算、guard-pinned 重试、证明阶梯、风险开关及重写后的模型交接。[VERIFY: src/main.cpp:1275]
- 已核对 `MulAbstraction`、`AbstractionOptions`、`AbstractionStats` 的真实字段与语义。[VERIFY: src/abstraction.hpp:27]
- 已核对严格代数发现、采样指纹、字级/位级匹配、锥提取、约束常量证明、低位恒零证明及模型发射实现。[VERIFY: src/abstraction.cpp:396]
- 文中公式仅用于改写代码中明确实现的移位关系、模乘语义和分配律，没有引入代码之外的算法假设。[VERIFY: src/abstraction.hpp:7]
