BUTTRESS 面向结构差异显著的 datapath equivalence checking 问题，核心思想是将传统工具难以利用的 guarded correspondence 转化为经过形式证明的辅助引理，并逐步注入 miter 以促进证明收敛。其整体流程首先基于满足原始约束的 constraint-legal simulation 对 specification 与 implementation 两侧内部节点进行采样，通过 value fingerprint 自动提名可能存在的跨侧对应关系。候选关系不仅包括普通节点等价，还支持不同位宽、slice、zero/sign extension、固定或数据相关 shift，以及受控制条件约束的 guarded relation，从而能够描述优化 datapath 中常见的 bypass、width reduction、normalization 和算术重构等现象。需要强调的是，simulation 仅用于发现候选，不能作为任何模型变换的依据；每个候选关系都必须通过 exact algebraic identity 或 SMT solver 的 UNSAT proof 才能被正式采用。对于无条件成立的 correspondence，BUTTRESS 在满足结构安全条件后将其转换为 verified cut，通过节点重定向消除两侧重复的 arithmetic cone；对于仅在特定 guard 下成立的关系，则不进行节点合并，而是以 constraint 的形式注入原 miter，使 solver 能够直接利用这一已证明的内部语义联系，同时保持原模型的 SAT/UNSAT 结果不变。候选引理按照从输入到 property 的 logic level 逐步调度，每接受一个 lemma 或 cut 后均重新构建模型并再次发现候选，使后续更深层、更复杂的算术关系能够利用此前已经建立的证明结果。最终，经过逐步简化和强化的 miter 被交给任意 sound backend solver 完成最终的 equivalence checking。
