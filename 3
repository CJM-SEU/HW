这是 CentOS 7 缺少系统静态 glibc 库导致的，并不是 GMP 或 Bitwuzla 源码错误。

  Bitwuzla 在 --default-library=static 模式下，会给最终命令行程序添加：

  -static

  于是链接器要求系统提供静态版本的：

  - libm.a
  - libpthread.a
  - librt.a（你看到的最后一个可能是 -lrt）

  这些通常由 glibc-static 提供，但你没有 sudo，无法安装系统包。

  FORWORD 只要求 Bitwuzla 静态库，不要求 bitwuzla 命令行程序自身完全静态。因此，应保留 libbitwuzla.a，但取消命令行程序的
  -static。

  ## 1. 修改 Bitwuzla 的命令行程序配置

  export ROOT=/hongfeng/cwx1485722/code/forword_exp/FORWORD-HW-main

  cd "$ROOT/deps/smt-switch/deps/bitwuzla/src/main"

  cp meson.build meson.build.before-centos7

  把添加 -static 的行注释掉：

  sed -i \
    "s/^[[:space:]]*link_args += \\['-static'\\]/  # Disabled: CentOS host has no glibc-static/" \
    meson.build

  检查结果：

  grep -n -A3 -B3 'glibc-static\|-static' meson.build

  应看到类似：

  if build_static and host_machine.system() != 'darwin'
    # Disabled: CentOS host has no glibc-static
  endif

  ## 2. 重新配置

  为了避免旧链接参数继续留在 Ninja 文件中，清空构建目录：

  export PREFIX=$ROOT/.local-centos7
  export PYTHON38=/hongfeng/cwx1485722/python38/bin/python3.8
  export MESON_SRC=$ROOT/packages/meson-0.64.1

  export PATH="$PREFIX/bin:$PATH"
  export LD_LIBRARY_PATH="$PREFIX/lib64:$PREFIX/lib:${LD_LIBRARY_PATH:-}"
  export PKG_CONFIG_PATH="$PREFIX/lib/pkgconfig:$PREFIX/lib64/pkgconfig:${PKG_CONFIG_PATH:-}"

  rm -rf "$ROOT/host-build/bitwuzla"

  重新配置，仍保留静态库模式：

  "$PYTHON38" "$MESON_SRC/meson.py" setup \
    "$ROOT/host-build/bitwuzla" \
    "$ROOT/deps/smt-switch/deps/bitwuzla" \
    --prefix="$PREFIX" \
    --buildtype=release \
    --default-library=static \
    -Dtesting=disabled \
    -Dunit_testing=disabled \
    -Dpython=false \
    -Ddocs=false

  ## 3. 重新编译

  "$PYTHON38" "$MESON_SRC/meson.py" compile \
    -C "$ROOT/host-build/bitwuzla" \
    -j 1

  现在最终链接命令中不应再出现独立的：

  -static

  -lm、-lpthread 和 -lrt 会改为链接系统动态库。

  ## 4. 安装

  "$PYTHON38" "$MESON_SRC/meson.py" install \
    -C "$ROOT/host-build/bitwuzla"

  验证静态库：

  find "$PREFIX" -name 'libbitwuzla*.a' -print
  find "$PREFIX" -name 'bitwuzla.pc' -print

  验证命令行程序：

  "$PREFIX/bin/bitwuzla" --version
  ldd "$PREFIX/bin/bitwuzla"

  ldd 显示动态依赖是正常的。我们需要的是 Bitwuzla 库保持静态，并不要求它的命令行程序完全静态。
