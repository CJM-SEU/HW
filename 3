python3 - <<'PY'
import sys
print("python executable:", sys.executable)

import semver
print("semver repr:", semver)
print("semver spec:", semver.__spec__)
print("semver origin:", getattr(semver.__spec__, "origin", None))
print("semver path:", getattr(semver, "__path__", None))
print("has SemVer:", hasattr(semver, "SemVer"))
print("dir head:", dir(semver)[:30])
PY
