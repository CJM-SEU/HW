先检查服务器是否藏有其他 Python：

  for p in python3.7 python3.8 python3.9 python3.10 python3.11; do
      command -v "$p" >/dev/null 2>&1 && {
          echo "$p: $(command -v "$p")"
          "$p" --version
      }
  done
