如何验证linux上bitwuzla已经就绪？以及接下来做什么？


• 可以从文件、pkg-config、运行时依赖和求解功能四层验证 Bitwuzla。

  ## 1. 设置环境

  export ROOT=/hongfeng/cwx1485722/code/forword_exp/FORWORD-HW-main
  export PREFIX=$ROOT/.local-centos7

  export PATH="$PREFIX/bin:$PATH"
  export LD_LIBRARY_PATH="$PREFIX/lib64:$PREFIX/lib:${LD_LIBRARY_PATH:-}"
  export PKG_CONFIG_PATH="$PREFIX/lib/pkgconfig:$PREFIX/lib64/pkgconfig:$PREFIX/lib/x86_64-linux-gnu/pkgconfig:
  ${PKG_CONFIG_PATH:-}"

  ## 2. 检查安装文件

  find "$PREFIX" \
    \( -name 'libbitwuzla*.a' -o \
       -name 'bitwuzla.pc' -o \
       -path '*/bin/bitwuzla' \) \
    -print

  至少应找到：

  - libbitwuzla.a
  - bitwuzla.pc
  - 最好还有 bin/bitwuzla

  检查静态库：

  BITWUZLA_LIB=$(find "$PREFIX" -name libbitwuzla.a | head -1)

  file "$BITWUZLA_LIB"
  ar t "$BITWUZLA_LIB" | head

  file 应显示：

  current ar archive

  ## 3. 检查 pkg-config

  BITWUZLA_PC=$(find "$PREFIX" -name bitwuzla.pc | head -1)
  export PKG_CONFIG_PATH="$(dirname "$BITWUZLA_PC"):$PKG_CONFIG_PATH"

  pkg-config --modversion bitwuzla
  pkg-config --cflags bitwuzla
  pkg-config --libs bitwuzla
  pkg-config --libs --static bitwuzla

  版本应类似：

  0.7.0-dev

  检查是否残留本地旧路径：

  grep -R '/home/jiongming' \
    "$PREFIX/lib" \
    "$PREFIX/lib64" \
    2>/dev/null

  这条命令应没有输出。

  ## 4. 检查命令行程序

  "$PREFIX/bin/bitwuzla" --version
  ldd "$PREFIX/bin/bitwuzla"

  ldd 输出中不能出现：

  not found

  如果没有生成 $PREFIX/bin/bitwuzla，但静态库存在，仍可以供 FORWORD 使用，不过建议先确认安装步骤确实执行成功。

  ## 5. 做一个真实求解测试

  创建测试文件：

  cat >/tmp/test-bitwuzla.smt2 <<'EOF'
  (set-logic QF_BV)
  (declare-fun x () (_ BitVec 8))
  (assert (= x #x2a))
  (check-sat)
  (exit)
  EOF

  运行：

  "$PREFIX/bin/bitwuzla" /tmp/test-bitwuzla.smt2

  预期输出包含：

  sat

  再测试不可满足问题：

  cat >/tmp/test-bitwuzla-unsat.smt2 <<'EOF'
  (set-logic QF_BV)
  (declare-fun x () (_ BitVec 8))
  (assert (= x #x01))
  (assert (= x #x02))
  (check-sat)
  (exit)
  EOF

  "$PREFIX/bin/bitwuzla" /tmp/test-bitwuzla-unsat.smt2

  预期：

  unsat

  如果上述检查全部通过，Bitwuzla 就已经就绪。

  ## 后续构建顺序

  接下来建议按以下顺序：

  Boost
    ↓
  glog 和 btor2tools
    ↓
  smt-switch
    ↓
  FORWORD

  gflags 已经完成，不需要重做。

  ## 下一步：构建 Boost

  仓库现在包含解压后的 Boost 源码：

  export JOBS=$(nproc)

  cd "$ROOT/packages/boost_1_82_0"

  清除之前可能失败的 Boost.Build 状态：

  rm -rf bin.v2
  rm -f project-config.jam

  重新生成 b2：

  ./bootstrap.sh \
    --with-toolset=gcc \
    --prefix="$PREFIX"

  确认 b2 能运行：

  ./b2 --version

  编译 FORWORD 需要的两个库：

  ./b2 -j1 \
    --with-system \
    --with-filesystem \
    toolset=gcc \
    link=static \
    runtime-link=shared \
    threading=multi \
    variant=release \
    cxxflags="-O2 -fPIC" \
    install

  成功后验证：

  find "$PREFIX" \
    \( -name 'libboost_system.a' -o \
       -name 'libboost_filesystem.a' \) \
    -print

  ls "$PREFIX/include/boost/version.hpp"

  如果 Boost 再次失败，请使用 -j1 输出中的第一条 error: 作为排查起点。Boost 完成后，下一步是重新编译 glog 和 btor2tools。
