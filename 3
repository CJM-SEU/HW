这个错误是因为仓库中的 deps/gmp-6.3.0 同时包含了原机器遗留的 .lo、.o、.libs 等编译产物。复制源码时这些旧对象也被复制到了 /
  tmp，CentOS 的 libtool 无法识别原机器生成的 fib_table.lo。

  不需要处理 Boost。只清理 /tmp 中的 GMP 副本并重新构建即可，不会影响仓库原文件。

  ## 1. 重新创建干净的 GMP 源码副本

  export ROOT=/hongfeng/cwx1485722/code/forword_exp/FORWORD-HW-main
  export PREFIX=$ROOT/.local-centos7
  export JOBS=$(nproc)

  rm -rf /tmp/cwx-gmp-6.3.0-src
  rm -rf /tmp/cwx-gmp-6.3.0-build

  mkdir -p /tmp/cwx-gmp-6.3.0-src
  mkdir -p /tmp/cwx-gmp-6.3.0-build

  cp -a \
    "$ROOT/deps/gmp-6.3.0/." \
    /tmp/cwx-gmp-6.3.0-src/

  ## 2. 删除原机器的构建产物

  以下操作只针对 /tmp 副本：

  cd /tmp/cwx-gmp-6.3.0-src

  find . -type f \( \
    -name '*.lo' -o \
    -name '*.o' -o \
    -name '*.la' -o \
    -name '*.a' -o \
    -name '*.so' -o \
    -name '*.so.*' \
  \) -delete

  find . -type d -name '.libs' -prune -exec rm -rf {} +

  rm -f config.status config.cache config.log libtool
  find . -name config.cache -delete

  确认不再存在 fib_table.lo：

  find /tmp/cwx-gmp-6.3.0-src -name 'fib_table.lo'

  这条命令应没有输出。

  ## 3. 重新配置

  cd /tmp/cwx-gmp-6.3.0-build

  CFLAGS="-O2 -fPIC" \
  CXXFLAGS="-O2 -fPIC" \
  /tmp/cwx-gmp-6.3.0-src/configure \
    --prefix="$PREFIX" \
    --enable-static \
    --enable-shared \
    --with-pic

  配置完成后确认新的 libtool 已生成：

  ls -l /tmp/cwx-gmp-6.3.0-build/libtool

  ## 4. 先单线程构建

  为了避免并行任务掩盖第一处错误：

  cd /tmp/cwx-gmp-6.3.0-build
  make -j1

  成功后安装：

  make install

  ## 5. 验证

  find "$PREFIX" \
    \( -name 'gmp.h' -o \
       -name 'libgmp.a' -o \
       -name 'libgmp.so*' \) \
    -print

  至少应看到：

  .local-centos7/include/gmp.h
  .local-centos7/lib/libgmp.a

  如果仍出现某个 .lo is not a valid libtool object，说明仓库内的 GMP 源码目录混入构建产物过多。那时最稳妥的方案是把官方 gmp-
  6.3.0.tar.xz 加入 CJM-SEU/baobao，在 CentOS 直接从官方干净源码包解压构建。
