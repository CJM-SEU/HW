  cd /path/to/FORWORD-HW

  uname -a
  cat /etc/centos-release

  gcc --version
  g++ --version
  cmake --version
  make --version
  python3 --version 2>&1
  pkg-config --version 2>&1

  which gcc g++ cmake make python3 pkg-config
  which module 2>/dev/null
  module avail 2>&1 | head -100

  rpm -q boost-devel gmp-devel gflags-devel flex bison
  2>/dev/null

  find deps -maxdepth 3 -type f \( \
    -name 'libgmp.a' -o \
    -name 'libglog.a' -o \
    -name 'libsmt-switch.a' -o \
    -name 'libbtor2parser.a' \
  \) -print
