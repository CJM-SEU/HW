原因是 bmc 依赖动态库 libwasim.so，但运行时动态链接器没有在搜索路径中找到它。

  我检查了当前文件：

  - bmc：/home/jiongming/desktop/FORWORD/build/bmc
  - 动态库：/home/jiongming/desktop/FORWORD/build/libwasim.so

  目前直接执行 ldd /home/jiongming/desktop/FORWORD/build/bmc 已经可以正确解析该库。不过脚本存在一个路径隐患：

  BMC_BIN="./build/bmc"

  这里的 ./ 是执行脚本时的当前目录，并不是脚本所在目录。因此：

  cd /home/jiongming/desktop/FORWORD
  /home/jiongming/desktop/forword_exp/run_bmc_exp.sh

  会使用正确的 FORWORD/build/bmc；但从其他目录执行脚本，就可能使用错误的 build/bmc，或者直接找不到二进制。

  可以先这样运行：

  cd /home/jiongming/desktop/FORWORD
  export LD_LIBRARY_PATH=/home/jiongming/desktop/FORWORD/build:${LD_LIBRARY_PATH:-}
  /home/jiongming/desktop/forword_exp/run_bmc_exp.sh

  更可靠的脚本修复方式是改成绝对路径，并显式加入动态库目录：

  PROJECT_DIR="/home/jiongming/desktop/FORWORD"
  BMC_BIN="$PROJECT_DIR/build/bmc"
  export LD_LIBRARY_PATH="$PROJECT_DIR/build:${LD_LIBRARY_PATH:-}"

  可以用下面两条命令验证：

  ldd /home/jiongming/desktop/FORWORD/build/bmc | grep wasim
  readelf -d /home/jiongming/desktop/FORWORD/build/bmc | grep -E 'RPATH|RUNPATH'

  当前这台机器上的 bmc 内嵌了 /home/jiongming/desktop/FORWORD/build 的 RPATH，所以现在能够找到库。如果日志来自旧版本 bmc、复制到另一台机器的构建产物，或者项目路径
  发生过变化，旧的绝对 RPATH 就可能失效；重新编译或设置 LD_LIBRARY_PATH 即可解决。
